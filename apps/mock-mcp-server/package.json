{
  "name": "@aegis/mock-mcp-server",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aegis/protocol": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "@types/node": "^24.0.0"
  }
}
